<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.GestionEntidades"
        android:supportsRtl="true"
        tools:targetApi="31">

        <!-- Actividades principales -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad para gestionar a Papá Noel -->
        <activity
            android:name=".GestionarPapaNoelActivity"
            android:exported="false" />

        <activity
            android:name=".ListaPapaNoelActivity"
            android:exported="false" />

        <activity
            android:name=".ActualizarPapaNoelActivity"
            android:exported="false" />

        <!-- Actividades para gestionar los renos -->
        <activity
            android:name=".GestionarRenosActivity"
            android:exported="false" />

        <activity
            android:name=".ListaRenosActivity"
            android:exported="false" />

        <activity
            android:name=".ActualizarRenoActivity"
            android:exported="false" />

    </application>

</manifest>
